<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gxa.pipe.mapper.LogMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.gxa.pipe.pojo.entity.Log">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="type" property="type" jdbcType="INTEGER"/>
        <result column="operate" property="operate" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="ip_address" property="ipAddress" jdbcType="VARCHAR"/>
        <result column="details" property="details" jdbcType="VARCHAR"/>
        <result column="period" property="period" jdbcType="DECIMAL"/>
        <result column="operation_time" property="operationTime" jdbcType="TIMESTAMP"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        id, user_id, type, operate, status, ip_address, details, period, operation_time, create_time, update_time
    </sql>

    <!-- 分页查询日志 -->
    <select id="selectByPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM log
        <where>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="type != null">
                AND type = #{type}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="ipAddress != null and ipAddress != ''">
                AND ip_address = #{ipAddress}
            </if>
            <if test="startTime != null">
                AND operation_time >= #{startTime}
            </if>
            <if test="endTime != null">
                AND operation_time &lt;= #{endTime}
            </if>
        </where>
        ORDER BY operation_time DESC
    </select>

    <!-- 根据ID查询日志 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM log
        WHERE id = #{id}
    </select>

    <!-- 插入日志 -->
    <insert id="insert" parameterType="com.gxa.pipe.pojo.entity.Log" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO log (user_id, type, operate, status, ip_address, details, period, operation_time, create_time, update_time)
        VALUES (#{userId}, #{type}, #{operate}, #{status}, #{ipAddress}, #{details}, #{period}, #{operationTime}, #{createTime}, #{updateTime})
    </insert>

    <!-- 更新日志 -->
    <update id="update" parameterType="com.gxa.pipe.pojo.entity.Log">
        UPDATE log
        <set>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="type != null">type = #{type},</if>
            <if test="operate != null">operate = #{operate},</if>
            <if test="status != null">status = #{status},</if>
            <if test="ipAddress != null">ip_address = #{ipAddress},</if>
            <if test="details != null">details = #{details},</if>
            <if test="period != null">period = #{period},</if>
            <if test="operationTime != null">operation_time = #{operationTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 删除日志 -->
    <delete id="deleteById">
        DELETE FROM log WHERE id = #{id}
    </delete>

</mapper>