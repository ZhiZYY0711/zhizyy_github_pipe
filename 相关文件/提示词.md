我要制作一个油气管道监控系统 帮我生成登录界面组件

一、登录框放在中间靠右的地方 登录框中的内容：

1.下拉菜单形式 选择全局管理员或区域管理员

2.账号、密码、验证码表单 密码支持点击小眼睛可以查看和隐藏 

3.一个忘记密码点我的链接  先不写功能

4.登录按钮 点击登录后校验账号和密码格式 账号为6到12位的纯数字 密码为9-24位的包含数字 大写和小写字母 在使用axios的地方只写注释

二、背景区域深蓝色为主 左侧展示出系统名字 油气管道监控系统 鼠标在背景区域移动时 有一定动画效果


根据这个表设计帮我生成sql文件 并做如下要求：
1.各个表的主键不使用varchar 使用bigint和autoincrement
2.为每个表创建create_time和update_time字段
3.task.result可以设置默认值: DEFAULT 0
4.task.response_time和 accomplish_time: 明确 NULL约束
5.journal_sensor.overhaul_time: 明确 NULL约束
执行完上述新增要求后 按新增要求 修改表设计.md文件


帮我制作通用页面
功能需求：
1.页面分为顶部标题栏、左侧导航栏、右侧内容区域
2.左侧为导航栏 包含以下模块：数据可视化、数据分析、监测数据、设备信息、任务管理、检修员信息、日志记录
3.顶部为标题栏 包含系统名称和用户信息
样式需求：
1.标题栏和导航栏背景为深蓝色 文字颜色为白色
2.导航栏相应模块鼠标悬停时颜色加深 字体放大 必须带有动画效果
3.页面所有内容放在一页中 不要有滚动条


生成监测数据组件
功能需求：
1.搜索功能 下拉框选择搜索字段 可以多选 输入框输入搜索内容 支持模糊查询（不写具体实现）
2.表格字段 依据该sql文件设计 主要是inspection表并连接多表的信息 包含以下字段：inspection表所有字段、传感器id、区域、所属管道名、创建时间、更新时间
3.表格中每个字段有倒序功能 点击字段名可以切换排序方向
4.表格每页展示20条数据 下方有分页器 可以点击切换页面
5.功能按钮每条记录后有日志按钮（不写具体实现）
6.日志按钮后有其他功能按钮 点击从按钮处弹出菜单 包含删除和修改功能（不写具体实现）
7.表格上有添加按钮 点击添加按钮可以添加新的监测数据记录（不写具体实现）
8.导出功能 点击导出按钮可以导出当前表格数据到excel文件（不写具体实现）
样式需求：
1.表格中数据行鼠标悬停时背景颜色加深 必须带有动画效果
2.表格中数据行 奇数行背景颜色为白色 偶数行背景颜色为浅蓝色
